{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "postgres",
  "database": "task-management",
  "autoLoadEntities": true,
  "migrations": ["src/migration/**/*.ts"],
  "synchronize": true,
  "cli": {
    "migrationsDri": "src/migration"
  }
}
